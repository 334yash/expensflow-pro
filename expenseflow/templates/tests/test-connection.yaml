apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expenseflow.fullname" . }}-test-connection"
  labels:
    {{- include "expenseflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expenseflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
