apiVersion: v1
kind: Service
metadata:
  name: {{ include "expenseflow.fullname" . }}
  labels:
    {{- include "expenseflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expenseflow.selectorLabels" . | nindent 4 }}
